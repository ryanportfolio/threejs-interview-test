# Deployment

None. Local Vite dev server only; nothing is hosted.

The deliverable is the repo itself: https://github.com/ryanportfolio/threejs-interview-test (private mirror of vakaros/threejs-interview-test, upstream remote wired), plus DECISIONS.md, the prompt log, and the annotated `[ai]`/`[hand]`/`[ai+edit]` commit history. Submission details (deadline, address) are in the invitation email, outside this repo.
